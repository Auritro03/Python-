tp = (["POP JEE", 20002], ["SOP JEE", 20003], ["DOP JEE", 20004])
tp[0][1] = 0
print(tp)
# can't declare single element in tuple. Must need to use a comma (,) after the element if it is single element.

